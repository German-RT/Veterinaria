@model IEnumerable<Veterinaria.Models.Mascota>

@{
    ViewData["Title"] = "Lista de Mascotas";
}

<div class="container mt-5">
    <h2 class="text-center mb-4">🐾 Lista de Mascotas</h2>

    <table class="table table-striped table-bordered shadow-sm rounded">
        <thead class="table-dark">
            <tr>
                <th>Nombre</th>
                <th>Tipo</th>
                <th>Edad</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var mascota in Model)
            {
                <tr>
                    <td>@mascota.Nombre</td>
                    <td>@mascota.Tipo</td>
                    <td>@mascota.Edad años</td>
                </tr>
            }
        </tbody>
    </table>

    <div class="text-end">
        <a href="@Url.Action("Registrar", "Mascota")" class="btn btn-primary">
            ➕ Registrar nueva mascota
        </a>
    </div>
</div>